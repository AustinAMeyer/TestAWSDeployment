
version: 2.1
jobs:  
  reate_infrastructure:
    docker:
      - image: "amazon/aws-cli"
    steps:
      - print_pipeline_id:
          to: $CIRCLE_WORKFLOW_ID

      - run:
          aws cloudformation create-stack --stack-name "InfastructureCreation" --template-body file://template.yml --region=us-west-2

workflows:
  # Name the workflow "welcome"
  reate_infrastructure:
    # Run the welcome/run job in its own container
    jobs:
  #Printing out the commands that I have created in an enviroment variable
        -  create_infrastructure